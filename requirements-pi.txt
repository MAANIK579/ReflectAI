# Raspberry Pi-specific dependencies (camera, GPIO, audio hardware, etc.)
# will be added here as each phase introduces hardware-dependent packages.
# Kept separate from requirements.txt so Mac development never needs
# Pi-only packages installed.
-r requirements.txt
